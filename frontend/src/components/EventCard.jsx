import React from'react';import{CalendarDays,MapPin,ArrowRight}from'lucide-react';import{date}from'../api';
const covers=['cover-purple','cover-orange','cover-blue','cover-pink','cover-green'];
export default function EventCard({event,index=0,onOpen}){return <article className="event-card" onClick={()=>onOpen(event)} tabIndex="0" onKeyDown={e=>e.key==='Enter'&&onOpen(event)}><div className={'event-image '+covers[index%covers.length]}><span className="event-category">{event.categoria?.nome||'Evento'}</span><div className="event-art"><span>{(event.nome||'EVENTO').slice(0,18)}</span></div></div><div className="event-info"><div className="date-box"><strong>{new Date(event.dataHora).getDate()}</strong><span>{new Date(event.dataHora).toLocaleDateString('pt-BR',{month:'short'}).replace('.','')}</span></div><div className="event-copy"><h3>{event.nome}</h3><p><CalendarDays size={15}/>{date(event.dataHora)}</p><p><MapPin size={15}/>{event.local?.nome||'Local a definir'}</p></div><ArrowRight className="card-arrow" size={18}/></div></article>}
