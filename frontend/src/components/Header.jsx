import React from'react';import{Search,Ticket,LogOut,UserRound,LayoutDashboard}from'lucide-react';
export default function Header({user,view,setView,onLogout,query,setQuery}){return <><header className="market-header"><button className="logo" onClick={()=>setView('home')}><span className="logo-symbol">E</span><span>Event<span>Flow</span></span></button><div className="header-search"><Search size={18}/><input value={query} onChange={e=>setQuery(e.target.value)} onFocus={()=>setView('home')} placeholder="Busque por eventos, shows, cursos..."/></div><nav><button onClick={()=>setView('home')}>Eventos</button>{user?.tipoUsuario==='PARTICIPANTE'&&<button onClick={()=>setView('tickets')}><Ticket size={18}/>Meus ingressos</button>}{user?.tipoUsuario==='ORGANIZADOR'&&<button onClick={()=>setView('dashboard')}><LayoutDashboard size={18}/>Painel</button>}{user?<button className="profile" onClick={onLogout}><UserRound size={18}/>{user.nome?.split(' ')[0]}<LogOut size={15}/></button>:<button className="login-button" onClick={()=>setView('auth')}>Entrar</button>}</nav></header><div className="mobile-search"><Search size={18}/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Buscar eventos"/></div></>}
